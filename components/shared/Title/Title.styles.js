export default function titleStyles(theme) {
  return {
    root: { marginBottom: theme.spacing(2) },
    content: { marginBottom: theme.spacing(2) },
    title: { marginLeft: theme.spacing(2) },
    controls: { marginRight: theme.spacing(2) },
  };
}
