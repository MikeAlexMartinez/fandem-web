export default function titleStyles(theme) {
  return {
    root: { marginBottom: theme.spacing.unit * 2 },
    content: { marginBottom: theme.spacing.unit * 2 },
    title: { marginLeft: theme.spacing.unit * 2 },
    controls: { marginRight: theme.spacing.unit * 2 },
  };
}
